{
  "name": "whisper-messenger",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Meta voice message transcription using Whisper",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260217.0",
    "miniflare": "^4.20260217.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18",
    "vitest-environment-miniflare": "^2.14.4",
    "wrangler": "^4.80.0"
  },
  "overrides": {
    "undici": "^6.24.1"
  },
  "dependencies": {
    "lucide-preact": "^1.7.0",
    "preact": "^10.29.0",
    "preact-render-to-string": "^6.6.7"
  }
}