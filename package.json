{
  "name": "echo-messenger",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "node --watch --conditions=worker src/index.ts",
    "dev": "npx tsx src/server.ts",
    "test": "npx vitest run",
    "deploy:k8s": "bash scripts/deploy.sh",
    "deploy:tunnel": "bash scripts/deploy-with-tunnel.sh",
    "deploy:full": "bash scripts/deploy-with-tunnel.sh"
  },
  "type": "module",
  "dependencies": {
    "@preact/signals": "^1.2.3",
    "@preact/router": "^4.1.1"
  }
}
