{
  "name": "echo-messenger",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "node --watch --conditions=worker src/index.ts",
    "dev": "npx tsx src/server.ts",
    "test": "npx vitest run",
    "deploy:k8s": "bash scripts/deploy.sh",
    "cert:generate": "node scripts/generate-cloudflare-cert.js"
  },
  "type": "module",
  "dependencies": {
    "@preact/signals": "^1.2.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "preact": "^10.22.1",
    "preact-render-to-string": "^6.5.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "supertest": "^7.0.0",
    "vitest": "^4.1.5"
  }
}
