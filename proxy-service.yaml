apiVersion: v1
kind: Service
metadata:
  name: echo-frontend-proxy
  namespace: debugging-testcrash-cloud
spec:
  type: ExternalName
  externalName: echo-frontend.debugging-voicemsg.svc.cluster.local
  ports:
  - port: 80