apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: debugging-voicemsg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
      - name: grafana
        image: grafana/grafana
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: debugging-voicemsg
spec:
  selector:
    app: grafana
  ports:
  - port: 3000