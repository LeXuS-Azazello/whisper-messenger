apiVersion: v1
kind: Service
metadata:
  name: voicemsg-public
  namespace: debugging-testcrash-cloud
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 3000
  selector:
    app: echo-frontend