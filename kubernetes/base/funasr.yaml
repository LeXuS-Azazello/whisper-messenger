apiVersion: apps/v1
kind: Deployment
metadata:
  name: funasr
  namespace: debugging-testcrash-pub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: funasr
  template:
    metadata:
      labels:
        app: funasr
    spec:
      imagePullSecrets:
        - name: forgejo-pull-secret
      containers:
        - name: funasr-api
          image: forge.dev.takatan.cloud/lexus/voicemsg-net:funasr-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50001
          readinessProbe:
            httpGet:
              path: /ready
              port: 50001
            initialDelaySeconds: 180
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 60
          env:
            - name: FUNASR_WORKERS
              value: "1"
            - name: FUNASR_MODEL
              value: "FunAudioLLM/Fun-ASR-MLT-Nano-2512"
            - name: FUNASR_DEVICE
              value: "cpu"
            - name: FUNASR_NCPU
              value: "4"
            - name: OMP_NUM_THREADS
              value: "4"
            - name: MKL_NUM_THREADS
              value: "4"
            - name: OPENBLAS_NUM_THREADS
              value: "4"
            - name: NUMEXPR_NUM_THREADS
              value: "4"
            - name: TOKENIZERS_PARALLELISM
              value: "false"
            - name: FUNASR_MODEL_PATH
              value: "/models/FunAudioLLM/Fun-ASR-MLT-Nano-2512"
            - name: FUNASR_VAD_MODEL_PATH
              value: "/models/iic/speech_fsmn_vad_zh-cn-16k-common-pytorch"
            - name: FUNASR_PUNC_MODEL_PATH
              value: "/models/iic/punc_ct-transformer_cn-en-common-vocab471067-large"
            - name: FUNASR_DISABLE_UPDATE
              value: "true"
          resources:
            requests:
              cpu: "4"
              memory: "8Gi"
            limits:
              cpu: "4"
              memory: "12Gi"
          volumeMounts:
            - name: models
              mountPath: "/models"
            - name: temp-media
              mountPath: "/temporaly-media-msg"
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: funasr-model-pvc
        - name: temp-media
          persistentVolumeClaim:
            claimName: temporaly-media-msg
---
apiVersion: v1
kind: Service
metadata:
  name: funasr
  namespace: debugging-testcrash-pub
spec:
  selector:
    app: funasr
  ports:
    - port: 50001
      targetPort: 50001
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: funasr-model-pvc
  namespace: debugging-testcrash-pub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path