apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- frontend.yaml
- redis.yaml
- network-policy.yaml
- mtproto-bridge-manager.yaml
- qwen3-asr.yaml
- ingress.yaml
- ingress-nginx.yaml
- tg-client.yaml

# Monitoring (optional — comment out to exclude)
- grafana.yaml
- prometheus.yaml
- fluentd.yaml

namespace: debugging-testcrash-cloud
