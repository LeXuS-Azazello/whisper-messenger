# samesame Model Downloader Job
# One-time (or on-demand) Job that downloads the TTS models into the PVC used by the samesame Deployment.
apiVersion: batch/v1
kind: Job
metadata:
  generateName: samesame-model-downloader-
  namespace: debugging-testcrash-pub
  labels:
    app: samesame
    component: model-downloader
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: samesame
        component: model-downloader
    spec:
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: downloader
          image: harbor.dev.takatan.cloud/azazellosaraksh/samesame:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c", "/scripts/download-models.sh && /scripts/prepare_models.sh"]
          env:
            - name: MODELS_DIR
              value: "/models"
            - name: HUGGINGFACE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: token
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: token
            - name: DOWNLOAD_TIMEOUT
              value: "1800"
            - name: SAMESAME_MODEL_NAME
              value: "FunAudioLLM/Fun-CosyVoice3-0.5B-2512"
            - name: COQUI_TOS_AGREED
              value: "1"
          volumeMounts:
            - name: models
              mountPath: /models
            - name: scripts
              mountPath: /scripts
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: samesame-model-cache
        - name: scripts
          configMap:
            name: samesame-download-scripts
            defaultMode: 0755