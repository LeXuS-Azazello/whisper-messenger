apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: samesame-model-cache
  namespace: debugging-testcrash-pub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: samesame-download-scripts
  namespace: debugging-testcrash-pub
data:
  download-models.sh: |
    #!/bin/sh
    set -eu

    MODELS_DIR="${MODELS_DIR:-/models}"
    echo "[samesame-downloader] MODELS_DIR=$MODELS_DIR"
    mkdir -p "$MODELS_DIR"

    python /app/download_model.py "$MODELS_DIR"
---
apiVersion: batch/v1
kind: Job
metadata:
  name: samesame-model-downloader
  namespace: debugging-testcrash-pub
spec:
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: downloader
          image: harbor.dev.takatan.cloud/devcenter/samesame:latest
          command: ["/bin/sh", "-c", "/scripts/download-models.sh"]
          env:
            - name: MODELS_DIR
              value: "/models"
            - name: HUGGINGFACE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: HUGGING_FACE_HUB_TOKEN
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: HUGGING_FACE_HUB_TOKEN
          volumeMounts:
            - name: models
              mountPath: /models
            - name: scripts
              mountPath: /scripts
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: samesame-model-cache
        - name: scripts
          configMap:
            name: samesame-download-scripts
            defaultMode: 0755
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: samesame
  namespace: debugging-testcrash-pub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: samesame
  template:
    metadata:
      labels:
        app: samesame
    spec:
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: samesame
          image: harbor.dev.takatan.cloud/devcenter/samesame:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
              name: http
          env:
            - name: TTS_MODEL_DIR
              value: "/models"
            - name: HF_HOME
              value: "/models"
            - name: HUGGINGFACE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: HUGGING_FACE_HUB_TOKEN
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: HUGGING_FACE_HUB_TOKEN
            - name: SAMESAME_SECRET
              valueFrom:
                secretKeyRef:
                  name: samesame-secret
                  key: SAMESAME_SECRET
            - name: SAMESAME_LOCAL_ONLY
              value: "true"
            - name: SAMESAME_MODEL_NAME
              value: "tts_models/multilingual/multi-dataset/your_tts"
            - name: SAMESAME_VOCODER_NAME
              value: "vocoder_models/universal/libritts/fullband-melgan"
          resources:
            requests:
              cpu: "2000m"
              memory: "4Gi"
            limits:
              cpu: "4000m"
              memory: "8Gi"
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 10
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: samesame-model-cache
---
apiVersion: v1
kind: Service
metadata:
  name: samesame
  namespace: debugging-testcrash-pub
spec:
  selector:
    app: samesame
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
