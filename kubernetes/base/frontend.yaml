apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-frontend
  namespace: debugging-testcrash-pub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echo-frontend
  template:
    metadata:
      labels:
        app: echo-frontend
    spec:
      containers:
      - name: frontend
        image: azazellosaraksh/debugging-mtproto-bridge:v4
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: MODE
          value: "FRONTEND"
        - name: REDIS_URL
          value: "redis://redis:6379"
        - name: QWEN_ASR_URL
          value: "http://qwen3-asr:11434"
        - name: BRIDGE_URL
          value: "http://mtproto-bridge-manager:3000"
        - name: BRIDGE_SECRET
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: BRIDGE_SECRET
              optional: true
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: SESSION_SECRET
              optional: true
        - name: WORKER_URL
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: WORKER_URL
              optional: true
        - name: GOOGLE_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: GOOGLE_CLIENT_ID
              optional: true
        - name: GOOGLE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: GOOGLE_CLIENT_SECRET
              optional: true
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: echo-frontend
  namespace: debugging-testcrash-pub
spec:
  type: LoadBalancer
  selector:
    app: echo-frontend
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http