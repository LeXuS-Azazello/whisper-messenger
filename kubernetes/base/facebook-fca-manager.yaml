apiVersion: v1
kind: ConfigMap
metadata:
  name: fca-pod-template
data:
  pod-template.json: |
    {
      "apiVersion": "v1",
      "kind": "Pod",
      "metadata": {
        "name": "fca-user",
        "labels": {
          "app": "facebook-fca-client"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "facebook-fca-client",
            "image": "harbor.dev.takatan.cloud/devcenter/facebook-fca-client:latest",
            "imagePullPolicy": "Always",
            "env": [
              {
                "name": "WHISPER_PROVIDER",
                "value": "http://whisper-service-v2.debugging-testcrash-pub.svc.cluster.local:8000/v1/transcribe-base64"
              }
            ],
            "resources": {
              "requests": {
                "cpu": "100m",
                "memory": "128Mi"
              },
              "limits": {
                "cpu": "500m",
                "memory": "512Mi"
              }
            }
          }
        ],
        "imagePullSecrets": [
          {
            "name": "harbor-pull-secret"
          }
        ],
        "restartPolicy": "Never"
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: facebook-fca-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: facebook-fca-manager
  template:
    metadata:
      labels:
        app: facebook-fca-manager
    spec:
      serviceAccountName: voicemsg-client-manager
      imagePullSecrets:
      - name: harbor-pull-secret
      containers:
      - name: manager
        image: harbor.dev.takatan.cloud/devcenter/facebook-fca-manager:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
        env:
        - name: MODE
          value: "MANAGER"
        - name: PORT
          value: "3003"
        - name: SECRET
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: MANAGER_SECRET
        - name: REDIS_URL
          value: "redis://redis:6379"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: whisper-messenger-env
              key: MONGODB_URI
---
apiVersion: v1
kind: Service
metadata:
  name: facebook-fca-manager
spec:
  selector:
    app: facebook-fca-manager
  ports:
  - port: 3003
    targetPort: 3003
