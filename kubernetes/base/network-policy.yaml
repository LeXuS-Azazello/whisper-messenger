apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tg-client-manager-network-policy
spec:
  podSelector:
    matchExpressions:
    - {key: app, operator: In, values: [tg-client-manager, tg-client-user]}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - {}
  egress:
  - {}